spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # Table 'ACT_GE_PROPERTY' doesn't exist -> &nullCatalogMeansCurrent=true
    url: jdbc:mysql://127.0.0.1:3306/yeee_activity7?characterEncoding=UTF-8&useUnicode=true&useSSL=false&serverTimezone=Asia/Shanghai&nullCatalogMeansCurrent=true
    username: root
    password: root
# spring-activity
  activiti:
    history-level: full
    db-history-used: true
    # 自动部署验证设置:true-开启（默认）、false-关闭
    check-process-definitions: false